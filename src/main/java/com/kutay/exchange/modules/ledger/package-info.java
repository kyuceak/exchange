@ApplicationModule(
        id = "ledger",
        displayName = "Ledger Module",
        allowedDependencies = {"shared"})
package com.kutay.exchange.modules.ledger;

import org.springframework.modulith.ApplicationModule;
